- if @restaurants.errors.any?
  %section#errors
    %h2
      = pluralize @restaurants.errors.count, "error"
      prohibited this restaurant from being saved:
    %ul
      - @restaurants.errors.full_messages.each do |message|
        %li= message

= form_for @restaurants do |f|
  = f.label :name
  = f.text_field :name
  = f.submit
